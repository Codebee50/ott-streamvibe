import SectionWrapper from "../../components/SectionWrapper";

const SupportSection = () => {
  return (
    <SectionWrapper>
      <section className="w-full max-container min-h-[50vh] bg-page-black"></section>
    </SectionWrapper>
  );
};

export default SupportSection;
